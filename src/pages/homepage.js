import * as React from "react"
import { Link } from "gatsby"
import Layout from "../components/layout"

const HomePage = () => {
    return (
        <Layout pageHeading= 'Home' pageTitle = 'Hellaur'></Layout>
    )
}

export default HomePage